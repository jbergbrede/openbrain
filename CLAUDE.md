## Memory

- For anything memory-related (save, search, list, recall), use the `mcp__openbrain__*` MCP tools — these are the user's personal memory store.
- Only use the file-based Claude Code memory system (`MEMORY.md`) when the user explicitly asks for "claude memory" or "your memory".
