{
  "mcpServers": {
    "openbrain": {
      "type": "stdio",
      "command": "sh",
      "args": [
        "-c",
        "uv --directory $(dirname $(git rev-parse --git-common-dir)) run openbrain --mode mcp"
      ],
      "env": {}
    }
  }
}