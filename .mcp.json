{
  "mcpServers": {
    "openbrain": {
      "type": "stdio",
      "command": "sh",
      "args": [
        "-c",
        "uv --directory $(git rev-parse --show-toplevel) run openbrain --mode mcp"
      ],
      "env": {}
    }
  }
}