# Embedding / LLM
OPENAI_API_KEY=sk-...
GOOGLE_API_KEY=AIza...

# Claude Code
ANTHROPIC_API_KEY=sk-... 

# Postgres
POSTGRES_DSN=postgresql://openbrain:changeme@db:5432/openbrain
POSTGRES_PASSWORD=changeme

# Slack (only needed for --mode slack or --mode both)
SLACK_BOT_TOKEN=xoxb-...
SLACK_APP_TOKEN=xapp-...

# Optional: path to YAML config (default: config.yaml in cwd)
# OPENBRAIN_CONFIG=/app/config.yaml
